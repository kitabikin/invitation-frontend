import { atom } from 'jotai';

export const themeAtom = atom('theme-gold');
export const displayAtom = atom('block');
export const overflowYAtom = atom('hidden');
export const isPlayingAtom = atom(false);
